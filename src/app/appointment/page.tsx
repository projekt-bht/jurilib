export default function AppointmentPage() {
  // Hier muss der Login Kontext geprüft werden, damit die Page nur für eingeloggte Nutzer*innen zugänglich ist
  return <div>Appointment Page für eingeloggte Nutzer*innen</div>;
}
