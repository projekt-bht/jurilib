meta {
  name: Get Appointment
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/api/appointment/employee/{{process.env.EMPLOYEE_ID}}/{{process.env.APPOINTMENT_ID}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
