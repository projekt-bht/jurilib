meta {
  name: Patch Service
  type: http
  seq: 1
}

patch {
  url: {{baseURL}}/organization/{{process.env.ORGANIZATION_ID}}/services/{{process.env.SERVICE_ID}}
  body: json
  auth: inherit
}

body:json {
  {
    "title": "bla keks, irgendein Service"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
