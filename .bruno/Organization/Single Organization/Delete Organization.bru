meta {
  name: Delete Organization
  type: http
  seq: 2
  tags: [
    Organization
  ]
}

delete {
  url: {{baseURL}}/organization/:organizationID
  body: none
  auth: inherit
}

params:path {
  organizationID: {{organizationID}}
}

settings {
  encodeUrl: true
  timeout: 0
}
